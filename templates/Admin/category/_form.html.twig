{{ form_start(form) }}
<div class="container-fluid">
    <div class="row">
        <div class="mb-3">
            <label for="InputCategory" class="form-label">{{ form_label((form.name)) }}</label>
            {{ form_widget(form.name) }}
        </div>
        <button type="submit" class="btn btn-primary w-50" href="{{ path('app_admin_recipe_category_index') }}">Enregistrer</button>
    </div>
</div>
{{ form_end(form) }}

