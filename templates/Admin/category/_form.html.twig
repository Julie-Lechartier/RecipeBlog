{{ form_start(form) }}
<div class="container-fluid">
    <div class="row">
        <div class="mb-3">
            <label for="InputCategory" class="form-label">{{ form_label((form.name)) }}</label>
            {{ form_widget(form.name) }}
        </div>
        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-secondary w-25" href="{{ path('app_admin_recipe_category_index') }}">
                Enregistrer
            </button>
        </div>
    </div>
</div>
{{ form_end(form) }}

