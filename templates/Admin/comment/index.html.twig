{% extends "admin/base.html.twig" %}
{% block body %}
    <div class="container-fluid bg-white mt-2 px-5">
        <div class="row mb-4 pt-4">
            <div class="col-md-8">
                <h2 class="fw-bold mb-0">Gestion des commentaires</h2>
                <p class="text-muted">Gérez tous les commentaires de la plateforme</p>
            </div>
            <div class="col-md-4 text-end">
                <a href="{{ path('app_admin_comment_new') }}" type="button"
                   class="btn btn-full-green text-white rounded-pill py-2 px-4 shadow-sm text-nowrap">
                    <i class="bi bi-chat-dots-fill me-2"></i>
                    Ajouter un commentaire
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-md-4">
                <label for="search" class="form-label small text-muted">Rechercher un commentaire</label>
                <div class="input-group">
                    <span class="input-group-text bg-white border-end-0">
                        <i class="bi bi-search text-muted"></i>
                    </span>
                    <input type="text" id="search" name="search" class="form-control border-start-0"
                           placeholder="Auteur, recette, contenu..."
                           hx-post=""
                           hx-target="#table-container"
                           hx-trigger="keyup changed delay:500ms"
                           hx-indicator=".htmx-indicator"
                           hx-include="[name='page']">
                </div>
            </div>
            <div class="col-md-3">
                <label for="recipe" class="form-label small text-muted">Filtrer par recette</label>
                <select id="recipe" name="recipe" class="form-select"
                        hx-post=""
                        hx-target="#table-container"
                        hx-trigger="change"
                        hx-include="[name='page']">
                    <option value="">Toutes les recettes</option>
                    {% if recipes is defined %}
                        {% for recipe in recipes %}
                            <option value="{{ recipe.id }}">{{ recipe.title }}</option>
                        {% endfor %}
                    {% endif %}
                </select>
            </div>
            <input type="hidden" name="page" value="{{ pagination.currentPageNumber|default(1) }}">

            <span class="htmx-indicator">Chargement...</span>
        </div>

        <div class="col-12" id="table-container">
            {% include 'admin/comment/_table.html.twig' %}
        </div>
    </div>

    <script>
        document.addEventListener('htmx:afterSwap', function (event) {
            const paginationLinks = document.querySelectorAll('.pagination a');
            paginationLinks.forEach(link => {
                link.addEventListener('click', function (e) {
                    e.preventDefault();
                    const page = new URLSearchParams(this.getAttribute('href')).get('page') || 1;
                    document.querySelector('input[name="page"]').value = page;
                    htmx.trigger('input[name="search"]', 'change');
                });
            });
        });
    </script>
{% endblock %}