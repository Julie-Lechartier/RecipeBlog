{% if pagination is defined and pagination.items is not empty %}
    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col" class="col-3">Titre</th>
            <th scope="col" class="col-3">Type</th>
            <th scope="col" class="col-3">Auteur</th>
            <th scope="col" class="col-3">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for recipe in pagination.items %}
            <tr>
                <th scope="row">{{ recipe.title }}</th>
                <td>
                    {% if recipe.category is empty %}
                        <span>Aucune catégorie</span>
                    {% else %}
                        {% for category in recipe.category %}
                            <span>{{ category.name }}</span>
                        {% endfor %}
                    {% endif %}
                </td>
                <td>{{ recipe.author.username }}</td>
                <td>
                    <div class="d-flex gap-2 pe-3">
                        <a href="{{ path('app_admin_recipe_edit', {'slug': recipe.slug}) }}"
                           class="btn btn-sm btn-outline-secondary rounded-pill px-3 py-2">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <a href="{{ path('app_admin_comment_new') }}"
                           class="btn btn-sm btn-outline-secondary rounded-pill px-3 py-2">
                            <i class="bi bi-chat"></i>
                        </a>
                        <button type="button"
                                class="btn-border-cherry rounded-pill px-3 py-2"
                                data-bs-toggle="modal" data-bs-target="#deleteModal{{ recipe.slug|replace({'-': ''}) }}">
                            <i class="bi bi-trash3"></i>
                        </button>
                    </div>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="mt-3">
        {{ knp_pagination_render(pagination, 'Admin/components/pagination.html.twig') }}
    </div>
{% else %}
    <p>Aucune recette disponible.</p>
{% endif %}