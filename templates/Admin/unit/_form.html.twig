{{ form_start(form) }}
<div class="container-fluid">
    <div class="row">
        <div class="mb-3 col-12">
            <label for="InputCategory" class="form-label">{{ form_label(form.name) }}</label>
            {{ form_widget(form.name) }}
        </div>
        <div>
            <button type="submit" class="btn w-25 btn-secondary" href="{{ path('app_admin_unit_index') }}">
                Enregistrer
            </button>
        </div>
    </div>
</div>
{{ form_end(form) }}