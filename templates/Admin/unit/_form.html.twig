{{ form_start(form) }}
<div class="container-fluid">
    <div class="row">
        <div class="mb-3 col-12">
            <label for="InputCategory" class="form-label">{{ form_label(form.name) }}</label>
            {{ form_widget(form.name) }}
        </div>
        <button type="submit" class="btn w-25 btn-full-brown" href="{{ path('app_admin_unit_index') }}">
            Enregistrer
        </button>
    </div>
</div>
{{ form_end(form) }}